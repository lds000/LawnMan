<UserControl x:Class="BackyardBoss.Views.ProgramEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:BackyardBoss.ViewModels"
             mc:Ignorable="d"
             Height="700" Width="1024">

    <UserControl.DataContext>
        <viewModels:ProgramEditorViewModel />
    </UserControl.DataContext>

    <Grid Background="#F0F2F5" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Global Adjustment -->
        <Border Background="White" Padding="10" CornerRadius="8" Margin="0,0,0,10">
            <Border.Effect>
                <DropShadowEffect Color="Gray" BlurRadius="8" ShadowDepth="2" />
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Global Seasonal Adjustment (%):" VerticalAlignment="Center" FontWeight="Bold" FontSize="16" />
                <TextBox Text="{Binding Schedule.SeasonalAdjustment, UpdateSourceTrigger=PropertyChanged}" Width="80" Margin="10,0,0,0" FontSize="16" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <!-- Left Panel -->
            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Programs" FontWeight="Bold" FontSize="18" Margin="0,0,0,10" />

                <DataGrid ItemsSource="{Binding Programs}" SelectedItem="{Binding SelectedProgram}" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" IsReadOnly="False"
                          FontSize="16" Margin="0,0,0,10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Title" Binding="{Binding Name}" Width="3*" />
                        <DataGridTemplateColumn Header="Enabled" Width="1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <Button Content="Add Program" Command="{Binding AddProgramCommand}" Width="150" Height="40" Margin="5" FontSize="16"/>
                    <Button Content="Remove Program" Command="{Binding RemoveProgramCommand}" Width="150" Height="40" Margin="5" FontSize="16" IsEnabled="{Binding IsProgramSelected}"/>
                </StackPanel>

                <TextBlock Text="Upcoming Runs" FontWeight="Bold" FontSize="18" Margin="0,10,0,5" />
                <DataGrid ItemsSource="{Binding UpcomingRuns}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                          FontSize="16" MinHeight="150" RowHeaderWidth="0" Background="#001f3f" Foreground="White" Margin="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Zone" Binding="{Binding SetName}" Width="*" />
                        <DataGridTextColumn Header="Start" Binding="{Binding StartTime}" Width="*" />
                        <DataGridTextColumn Header="Duration" Binding="{Binding RunDurationMinutes}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>

                <Button Content="Save Schedule" Command="{Binding SaveScheduleCommand}" Width="180" Height="40" Margin="10,10,0,0" HorizontalAlignment="Right" FontSize="16"/>
            </StackPanel>

            <!-- Right Panel -->
            <StackPanel Grid.Column="1">
                <TextBlock Text="Program Details" HorizontalAlignment="Center" FontWeight="Bold" FontSize="18" Margin="0,0,0,10" />

                <StackPanel Orientation="Vertical" Margin="0,0,0,10">
                    <TextBlock Text="Name:" FontSize="16" />
                    <TextBox Text="{Binding SelectedProgram.Name, UpdateSourceTrigger=PropertyChanged}" FontSize="16" Height="30" Margin="0,5" />
                </StackPanel>

                <TextBlock Text="Start Times" FontWeight="Bold" FontSize="16" Margin="0,10,0,5" />
                <ListBox ItemsSource="{Binding SelectedProgram.StartTimes}" SelectedItem="{Binding SelectedStartTime, Mode=TwoWay}"
                         Height="120" FontSize="16" />
                <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                    <Button Content="Add Start Time" Command="{Binding AddStartTimeCommand}" Width="150" Height="40" Margin="5" FontSize="16" IsEnabled="{Binding IsProgramSelected}"/>
                    <Button Content="Remove Start Time" Command="{Binding RemoveStartTimeCommand}" Width="150" Height="40" Margin="5" FontSize="16" IsEnabled="{Binding IsStartTimeSelected}"/>
                </StackPanel>

                <TextBlock Text="Sets (Zones)" FontWeight="Bold" FontSize="16" Margin="0,10,0,5" />
                <DataGrid ItemsSource="{Binding SelectedProgram.Sets}" SelectedItem="{Binding SelectedSet}" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" IsReadOnly="False" Height="160" FontSize="16">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Zone Name" Binding="{Binding SetName}" Width="3*" />
                        <DataGridTextColumn Header="Run Time (min)" Binding="{Binding RunDurationMinutes}" Width="2*" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>